# CLAUDE.md

## 프로젝트

기와체인(GIWA Chain)의 **가격 데이터 레이어**와, 그 위에 올리는 **업비트 유저용 온체인 터미널**.

2층 구조로 본다.

```
[하단] 인덱서 + 가격 API        ← 핵심 자산. 기와 생태계의 가격 표준을 먼저 정의한다
[상단] 터미널 + 검증 런치패드   ← 하단 API의 첫 번째 소비자이자 쇼케이스
```

**타겟 유저**: 업비트에서 큰 현물을 오래 들고 있지만 온체인 경험이 없는 사람("업저씨"). 밈코인 트레이더가 **아니다**.

**제품명 (2026-07-21 확정)**: 나루(NARU) — "업비트에서 기와 온체인으로 오는 나룻길". 로고·비주얼은 나루터(나룻배·물결) 모티프. 색 세계관은 "원목 마루 위의 터미널": 화면 전체가 마루널 타일(`apps/web/public/wood-floor.svg` — 판자 홈·엇갈린 이음매·널별 톤·결, 심리스 SVG, 중간톤) + 한지빛 텍스트 + **골든 오크 액센트(#C9A554, 보라·네이비 금지)**. 서피스는 유리판이 아니라 **음각(`.carved` 유틸리티)** — 나무를 파낸 홈(안그림자 + 아래 턱 하이라이트, 보더 없음)이고, 표의 행 구분은 검은 홈줄·헤더행은 인두 띠다. 나무가 모든 서피스 아래로 흐르며, 가독성은 음각 그늘 농도로 확보한다. 액센트는 하락 빨강과 색각이상 축이 가까우므로 데이터 인코딩에 쓰지 않는다(UI 크롬 전용). 상승/하락 색은 초록/빨강(국제 관례)으로 확정 — 임의로 뒤집지 않는다.

**제출 맥락**: 두나무 가속(GASOK) 2차. 1차 마감 2026-07-31, 실제 승부는 2026-09 KBW2026 무대.
제출물 = 피치덱 / 접속 가능한 웹사이트 or 데모 영상 / 컨트랙트 퍼블릭 링크 / 기술 문서.

---

## 절대 규칙

어기면 프로젝트 목적 자체가 깨진다. 코드 짜기 전에 매번 확인할 것.

### 1. 원화는 "환산 표시"일 뿐이다

기와에 원화 스테이블코인은 **존재하지 않는다**. 화면의 원화는 ETH 기준 가격을 오프체인 환율로 환산한 값이다.

- 온체인 값과 환산 값을 타입 레벨에서 분리한다 (`WeiAmount` vs `DisplayKrw`)
- UI에 환산 표시임을 알 수 있는 라벨을 남긴다
- 원화 잔고가 온체인에 있는 것처럼 보이는 UI는 만들지 않는다

### 2. 금융상품 표현 금지

두나무 심사 대상이고, 디지털자산기본법은 아직 입법 전이다.

- 금지: 예금, 이자, 수익률 보장, 원금 보장, 확정 APY
- 대안: "예치", "참여", "변동 가능" 같은 중립 표현
- 카피 문구를 새로 쓸 때는 이 규칙을 먼저 확인한다

### 3. 정보 밀도를 낮춘다

일반 DEX 스캐너의 반대로 간다. 정보 밀도가 곧 이탈률인 유저층이다.

| 금지 | 대신 |
|---|---|
| `$0.0₅5398` 지수 표기 | 일반 소수점 표기 |
| 토큰 수백 개 무한 스크롤 | 검증 자산만 수십 종 이내, 무한 스크롤 금지 (2026-07-22: 10개 미만 → 완화, 현 데모 20종) |
| 5M / 1H / 6H 변동률 | 일 · 월 단위 |
| MCAP, LIQUIDITY, FDV 같은 용어 | "예치 규모", "참여 인원" |
| 지갑 연결 강요 | 이메일 로그인 우선 동선 |

### 4. 체인 파라미터는 전부 주입식

프라이빗 메인넷 초대 시 **config 교체만으로 즉시 재싱크**되어야 한다. 이건 기술 문서에도 명시하는 세일즈 포인트다.

- RPC URL, chainId, 팩토리 주소, 시작 블록, 익스플로러 URL → 전부 env
- 주소나 chainId를 코드에 하드코딩하지 않는다
- 체인별 설정은 `config/chains.ts` 한 곳에서만 관리

### 5. 데모 시드 데이터는 숨기지 않는다

테스트넷 화면을 채우기 위해 봇으로 스왑을 돌린다. 이건 감추면 안 된다.

- 기술 문서와 UI 어딘가에 테스트넷 시드 데이터임을 명시
- 시드 생성 스크립트는 `scripts/seed/`에 분리, 프로덕션 번들에 포함시키지 않는다

### 6. 신원 검증 배지는 1급 시민

기와는 익명 퍼미션리스 체인이 아니라 신원 기반 규제 준수 체인이다(도장, GIWA ID, 보자기 전부 신원 관련). 검증 배지는 나중에 붙이는 장식이 아니라 스키마 설계 시점부터 자리를 잡는다.

---

## 체인 정보

| 항목 | GIWA Sepolia |
|---|---|
| Chain ID | 91342 |
| RPC | https://sepolia-rpc.giwa.io (rate-limited, 프로덕션 사용 금지) |
| Flashblocks RPC | https://sepolia-rpc-flashblocks.giwa.io |
| 통화 | ETH |
| 익스플로러 | https://sepolia-explorer.giwa.io (Blockscout) |
| 블록타임 | 1초 |

- 퍼블릭 메인넷 **미출시**. 가속 선정팀만 화이트리스트 프라이빗 메인넷 접근
- OP Stack 기반 옵티미스틱 롤업, 단일 시퀀서
- 공개 RPC는 레이트리밋 → 인덱서는 Nodit(람다256) 엔드포인트 사용
- Blockscout API는 토큰 메타/홀더/트랜스퍼만 제공. **가격·거래량·유동성은 직접 계산해야 한다**

---

## 스택

- 모노레포: pnpm workspace
- 인덱서: Ponder (viem 기반, 리오그 처리 내장)
- DB: PostgreSQL. 캔들은 `candles_1m` 원본 → 상위 간격 롤업
- API: Hono 또는 Next.js Route Handler, Redis 캐시
- 프론트: Next.js App Router + TypeScript
- 체인 연동: viem / wagmi (커스텀 체인 정의)
- 테이블: TanStack Table, 차트: lightweight-charts
- 실시간: Nodit Webhook/Stream 또는 SSE. 전체 리페치 금지, 델타만 푸시

---

## 디렉토리

```
apps/
  web/          프론트엔드
  api/          가격 API 서버
packages/
  indexer/      Ponder 인덱서
  contracts/    Foundry 프로젝트
  shared/       타입, 지표 계산 로직 (인덱서/API 공용)
config/
  chains.ts     체인 파라미터 단일 소스
scripts/
  seed/         데모 토큰 발행 + 스왑 봇
```

---

## 지표 정의

여기서 틀리면 전부 틀린다. 새 지표를 추가할 때는 반드시 이 섹션에 정의를 먼저 적는다.

### 가격
- V2: `reserve1 / reserve0`, 양쪽 decimals 보정 필수
- 스왑 이벤트마다 재계산해서 캔들에 반영. 구현: 같은 tx 에서 Sync 가 Swap 보다 먼저 나오므로(V2 `_update` 순서), Sync 핸들러가 준비금·가격을 갱신하고 Swap 핸들러가 그 가격으로 체결·캔들을 기록한다 (`packages/indexer`)
- 캔들 저장: `candles_1m` 원본과 1h/1d 롤업을 스왑 시점에 같은 트랜잭션으로 upsert. 일봉 버킷은 00:00 UTC = 09:00 KST — 업비트 일봉과 같은 경계라 그대로 쓴다
- **모든 화폐 계산은 bigint.** float 금지. 표시 직전에만 문자열로 변환

### USD / KRW 환산
- 온체인에 오라클 없음. 기준 풀(WETH-테스트스테이블) 하나를 quote로 고정
- ETH/KRW는 업비트 시세 API(`api.upbit.com/v1/ticker?markets=KRW-ETH`)에서 주입, 캐시 TTL 60초
  - 업비트 시세를 기준으로 쓰는 건 타겟 유저에게 익숙한 숫자를 보여주기 위한 **의도적 선택**이다. 피치 소재이기도 하므로 다른 소스로 바꾸지 않는다
- 환산 실패 시 원화 표시를 감추고 ETH 단위로 폴백. **추정치를 확정값처럼 보여주지 않는다**
- 환산 값은 `DisplayKrw`(0.001원 단위 bigint, `packages/shared`)로 다룬다. 표시 규칙: ≥1,000원 정수+콤마 / 100~1,000원 소수 1자리 / <100원 소수 2자리, **항상 내림**(과대 표시 금지). 축약은 만/억 단위(`formatKrwCompact`)

### 시가총액
`totalSupply × price`. 소각 주소와 락업 물량 제외 여부를 결정하고 그 정책을 코드 주석에 남긴다.

### 유동성
풀이 보유한 quote 토큰 잔고 × 2 (V2 기준)

### 거래자 수
윈도우 내 **distinct `tx.origin`**. 라우터 경유 때문에 `msg.sender`를 쓰면 라우터 주소 하나로 집계된다.

### 거래량 / 변동률
- 거래량: 윈도우 내 스왑의 quote 토큰 환산 합계
- 변동률: 캔들 open 대비 종가. **요청 시점에 스왑 원장을 스캔하지 않는다**

---

## 컨트랙트

- Foundry 사용
- Uniswap V2 포크(Factory / Pair / Router) + 토큰 발행 게이트
- **런치패드는 제품이다 (2026-07-21 팀 결정).** 기와 생태계에 아직 런치패드가 없다 — 선점한다. 단, 퍼미션리스가 아니라 **신원 검증 발행**이 정체성이다
  - 검증된 신원(도장 어테스테이션 / GIWA ID)만 발행 게이트를 통과한다. 검증 없는 즉시 상장 UI는 만들지 않는다
  - 발행 동선은 신청 → 검증 → 상장 흐름이다. "즉시 풀 생성" 같은 카피는 쓰지 않는다
  - 데모 단계의 프론트 발행은 시뮬레이션이며 화면에 명시한다. 온체인 발행은 컨트랙트 배포 후 연결한다
  - 발행 게이트 컨트랙트(TokenFactory)는 데모 시드 생성과 런치패드 제품이 같은 경로를 쓴다
- 배포 후 **Blockscout verify까지 완료해야 제출물로서 의미가 있다**
- 배포 주소는 `config/chains.ts`에 기록, 코드에 흩뿌리지 않는다
- 심사 제출용이므로 컨트랙트에 한글 주석으로 의도를 남겨둔다

---

## 코딩 컨벤션

- TypeScript strict. `any` 금지
- 금액은 bigint, 화면 표시 직전에만 포맷팅
- 인덱서는 멱등하게. 같은 블록을 두 번 처리해도 결과가 같아야 한다
- `eth_getLogs`는 블록 범위를 청크로 나눠 호출한다. 전체 범위 단발 호출은 거부당한다
- 커밋 메시지는 한글 가능, 코드 식별자는 영문
- 프론트 상태는 React state로. 브라우저 스토리지 API는 쓰지 않는다

---

## 명령어

```bash
pnpm dev            # 전체 개발 서버
pnpm indexer:dev    # 인덱서만
pnpm seed           # 데모 토큰 발행 + 스왑 봇 기동
pnpm typecheck
forge test          # packages/contracts 내부
```

---

## 현재 단계

가속 2차 제출 준비. 우선순위 순서:

1. 컨트랙트 배포 + verify
2. 데모 토큰 발행 + 스왑 봇 상시 가동 ← **빈 테이블로 제출하면 끝이다**
3. 인덱서 + 가격 API
4. 터미널 첫 화면
5. 데모 영상, 피치덱, 기술 문서

완성도보다 **돌아가는 뼈대 + 선명한 방향**이 먼저다.

---

## 보안

시드 봇 때문에 키를 여러 개 굴리게 된다. 사고 나기 쉬운 지점이다.

- 개인키 / 니모닉 / API 키를 커밋하지 않는다. 저장소에는 `.env.example`만 둔다
- **배포 지갑과 봇 지갑을 분리한다.** 봇 키는 테스트넷 전용이고 어떤 경우에도 메인넷 자산을 쥐지 않는다
- 봇 키를 프론트 번들이나 클라이언트 사이드 코드에 절대 넣지 않는다
- 심사 제출용 저장소를 공개할 때 커밋 히스토리에 키가 남아 있는지 먼저 확인한다

---

## 인덱싱 참고

주요 이벤트 topic0 (keccak256 계산값):

```
PairCreated(address,address,address,uint256)
  0x0d3648bd0f6ba80134a33ba9275ac585d9d315f0ad8355cddefde31afa28d0e9

Swap(address,uint256,uint256,uint256,uint256,address)   # V2
  0xd78ad95fa46c994b6551d0da85fc275fe613ce37657fb8d5e3d130840159d822

Sync(uint112,uint112)                                   # V2 준비금 갱신 — 가격 재계산 트리거
  0x1c411e9a96e071241c2f21f7726b17ae89e3cab4c78be50e062b03a9fffbbad1

Transfer(address,address,uint256)
  0xddf252ad1be2c89b69c2b068fc378daa952ba7f163c4a11628f55a4df523b3ef
```

새 이벤트를 추가할 때는 시그니처를 추측하지 말고 직접 keccak256을 계산해 확인한다.

---

## 용어

이 프로젝트 고유 용어라 문맥 없이는 알 수 없다.

| 용어 | 설명 |
|---|---|
| 업저씨 | 업비트에서 거래대금이 큰 현물 위주 홀더. 온체인 경험이 거의 없다. **이 프로젝트의 타겟** |
| 기와(GIWA) | 두나무의 웹3 인프라 브랜드. 기와체인 + 기와월렛으로 구성 |
| 가속(GASOK) | 두나무 개발자 지원 프로그램. 이 프로젝트의 제출처 |
| 도장(Dojang) | 오프체인 데이터를 온체인에서 검증하는 기와 어테스테이션 레이어 |
| GIWA ID | ENS 기반 네임. 양도 불가 SBT이며 KYC 검증 주소에 종속된다 |
| 보자기(Bojagi) | 기와의 영지식증명 프라이버시 프로토콜 |
| 두나무 | 업비트 운영사. 기와의 개발·운영 주체이자 심사 주체 |

---

## Claude에게

- 새 기능을 제안할 때 위 절대 규칙 6개와 충돌하는지 먼저 확인한다
- 타겟 유저가 업저씨라는 걸 잊지 않는다. 크립토 네이티브용 편의 기능은 대부분 불필요하다
- 지표 계산 코드를 수정할 때는 "지표 정의" 섹션도 같이 업데이트한다
- 결정을 임의로 바꾸지 말고, 바꿔야 한다고 판단되면 이유와 함께 먼저 묻는다
- 마감이 촉박하다. 완벽한 추상화보다 동작하는 구현을 우선한다
