# CLAUDE.md

## 프로젝트

기와체인(GIWA Chain)의 **가격 데이터 레이어**와, 그 위에 올리는 **업비트 유저용 온체인 터미널**.

2층 구조로 본다.

```
[하단] 인덱서 + 가격 API        ← 핵심 자산. 기와 생태계의 가격 표준을 먼저 정의한다
[상단] 터미널 + 검증 런치패드   ← 하단 API의 첫 번째 소비자이자 쇼케이스
```

**타겟 유저**: 업비트에서 큰 현물을 오래 들고 있지만 온체인 경험이 없는 사람("업저씨"). 밈코인 트레이더가 **아니다**.

**제품명 (2026-07-21 확정)**: 나루(NARU) — "업비트에서 기와체인으로 오는 나룻길" (2026-07-27: 화면 카피에서 "기와 온체인" → "기와체인" — 타겟 유저에게 "온체인"이 낯선 말이다). 로고·비주얼은 나루터(나룻배·물결) 모티프. 색 세계관은 "원목 마루 위의 터미널": 화면 전체가 마루널 타일(`apps/web/public/wood-floor.svg` — 판자 홈·엇갈린 이음매·널별 톤·결, 심리스 SVG, 중간톤) + 한지빛 텍스트 + **골든 오크 액센트(#C9A554, 보라·네이비 금지)**. 서피스는 유리판이 아니라 **음각(`.carved` 유틸리티)** — 나무를 파낸 홈(안그림자 + 아래 턱 하이라이트, 보더 없음)이고, 표의 행 구분은 검은 홈줄·헤더행은 인두 띠다. 나무가 모든 서피스 아래로 흐르며, 가독성은 음각 그늘 농도로 확보한다. 액센트는 하락 빨강과 색각이상 축이 가까우므로 데이터 인코딩에 쓰지 않는다(UI 크롬 전용). 상승/하락 색은 초록/빨강(국제 관례)으로 확정 — 임의로 뒤집지 않는다.

**제출 맥락**: 두나무 가속(GASOK) 2차. 1차 마감 2026-07-31, 실제 승부는 2026-09 KBW2026 무대.
제출물 = 피치덱 / 접속 가능한 웹사이트 or 데모 영상 / 컨트랙트 퍼블릭 링크 / 기술 문서.

---

## 절대 규칙

어기면 프로젝트 목적 자체가 깨진다. 코드 짜기 전에 매번 확인할 것.

### 1. 원화는 "환산 표시"일 뿐이다

기와에 원화 스테이블코인은 **존재하지 않는다**. 화면의 원화는 ETH 기준 가격을 오프체인 환율로 환산한 값이다.

- 온체인 값과 환산 값을 타입 레벨에서 분리한다 (`WeiAmount` vs `DisplayKrw`)
- UI에 환산 표시임을 알 수 있는 라벨을 남긴다
- 원화 잔고가 온체인에 있는 것처럼 보이는 UI는 만들지 않는다

### 2. 금융상품 표현 금지

두나무 심사 대상이고, 디지털자산기본법은 아직 입법 전이다.

- 금지: 예금, 이자, 수익률 보장, 원금 보장, 확정 APY
- 대안: "예치", "참여", "변동 가능" 같은 중립 표현
- 카피 문구를 새로 쓸 때는 이 규칙을 먼저 확인한다

### 3. 정보 밀도를 낮춘다

일반 DEX 스캐너의 반대로 간다. 정보 밀도가 곧 이탈률인 유저층이다.

| 금지 | 대신 |
|---|---|
| `$0.0₅5398` 지수 표기 | 일반 소수점 표기 |
| 토큰 수백 개 무한 스크롤 | 검증 자산만 수십 종 이내, 무한 스크롤 금지 (2026-07-22: 10개 미만 → 완화, 현 데모 19종) |
| 5M / 1H / 6H 변동률 | 일 · 월 단위 |
| MCAP, LIQUIDITY, FDV 같은 용어 | "예치 규모", "참여 인원" |
| 지갑 연결 강요 | 이메일 로그인 우선 동선 |

### 4. 체인 파라미터는 전부 주입식

프라이빗 메인넷 초대 시 **config 교체만으로 즉시 재싱크**되어야 한다. 이건 기술 문서에도 명시하는 세일즈 포인트다.

- RPC URL, chainId, 팩토리 주소, 시작 블록, 익스플로러 URL → 전부 env
- 주소나 chainId를 코드에 하드코딩하지 않는다
- 체인별 설정은 `config/chains.ts` 한 곳에서만 관리

### 5. 데모 시드 데이터는 숨기지 않는다

테스트넷 화면을 채우기 위해 봇으로 스왑을 돌린다. 이건 감추면 안 된다.

- 기술 문서와 UI 어딘가에 테스트넷 시드 데이터임을 명시
- 시드 생성 스크립트는 `scripts/seed/`에 분리, 프로덕션 번들에 포함시키지 않는다

### 6. 신원 검증 배지는 1급 시민

기와는 익명 퍼미션리스 체인이 아니라 신원 기반 규제 준수 체인이다(도장, GIWA ID, 보자기 전부 신원 관련). 검증 배지는 나중에 붙이는 장식이 아니라 스키마 설계 시점부터 자리를 잡는다.

- 배지 주체는 **"나루 검증"**으로 표기한다 — "GIWA 공식 인증"으로 오인될 문구 금지 (기획서 v1.3 §4.3). "검증은 사기를 거르는 절차일 뿐 투자 보증이 아니다"를 상시 고지한다 (2026-08-08: "사기 필터" → 평이체. 화면 문구의 단일 소스는 `apps/web/lib/site.ts`의 `VERIFICATION_LEAD` — 화면마다 손으로 쓰면 표현이 갈라지고 주장이 조용히 좁아진다)
- 배지 상태는 단건 조회 페이지로 외부에서 확인 가능하게 한다 (오프체인 사칭 방지)
- 현행 신원 원장은 도장 스키마를 준거한 **자체 IdentityRegistry**다. "도장 모듈 실사용"이라고 쓰지 않는다 — 정식 attester 등록은 기와팀 협의 중(미확정)

---

## 제품 결정 (기획서·개발 명세서 v1.3, 2026-07-26)

기획·전략의 단일 소스는 팀 기획서 v1.3, 기술 명세의 단일 소스는 개발 명세서 v1.3이다(작성 택혁). 코드에 영향을 주는 확정 사항만 여기에 옮긴다.

- **5면 구성**: 토큰 | 분석 [토큰 · 온체인 주식] | 발행 | 나루 포인트 | 내 자산. 현 구현은 토큰·발행·내 자산 3면 — 분석·포인트는 P1이며, 빈 자리표시 탭을 만들지 않는다(규칙 3)
- **수수료**: 메인넷 목표 총 0.8% 중 플랫폼 0.3%p(나머지 0.5%p는 LP 몫). 상장 대가는 받지 않는다. 테스트넷 현행은 V2 캐노니컬 0.3% 전량 LP 귀속 — 전환은 P2이고 법률 검토가 선행한다
- **나루 포인트**: 양도·환금 불가, 확률형 요소 금지(추첨 없는 확정형 배분). 참여 자격 = 업비트 KYC 인증 지갑, 인증 지갑당 계정 1개. "무료 배분" 같은 기대이익 암시 카피 금지(규칙 2의 연장)
- **온체인 주식 탭**: 철저히 조회 전용 — 거래 연결·가입 유도 금지, "파생상품이며 주식 소유권 아님" 상시 표기, 랭킹 라벨은 가치중립('PnL 상위'). 외부 데이터 원천 장애 시 해당 보드만 숨긴다(폴백 필수)
- **피드(나루터 소식)**: 코인니스식 사실 서술. 발행자 지갑 매도는 금액 무관 무조건 노출, 발행자에게서 물량 받은 지갑(1단계)까지 자동 확장 — 1단계 확장은 Transfer 전수 인덱싱(P1) 전제
- **미확정(확정처럼 쓰지 않는다)**: 업비트 1계정당 인증 지갑 수 정책, 나루의 도장 attester 등록 가능 여부, 테스트넷 UPBIT_KOREA 어테스테이션 실발급 여부, 메인넷 일정

개발 명세서 v1.3의 추가 확정:

- **07-31 제출까지 컨트랙트 재배포 금지.** 수수료는 현행 0.3% 전량 LP 유지. 전환은 라우터 수수료 파라미터 방식으로 테스트베드에서 배포·검증하되, 실가치 자산 환경이면 플랫폼 몫 0으로 배포(법률 검토 전 실적립 금지)
- **퍼블릭 API는 제공하지 않는다**(내부 API도 인증·오리진 제한 — P1 작업). 유일한 예외 = 배지 상태 단건 조회 페이지 `/verify/0x…`
- 랜딩에 "두나무가 만든 GIWA 체인 위에서 동작하는 독립 서비스이며 업비트·두나무와 제휴 관계가 아닙니다" 디스클레이머를 단다
- 데모 전용 장치(피드 하한 1만원 등)는 메인넷 빌드에서 제거한다
- **도장 조회는 퍼미션리스**(발급은 허가형). GIWA Sepolia `DojangScroll 0xd5077b67dcb56caC8b270C7788FC3E6ee03F17B9`, `isVerified(wallet, attesterId)`, attesterId(UPBIT_KOREA) = `keccak256("dojang.dojangattesterids.upbitkorea")` — 2026-07-26 온체인 실검증 완료(코드 존재·해시 일치·셀렉터 정상 응답). 주소·attesterId는 규칙 4에 따라 설정값으로만 다룬다(메인넷 주소 미공개)
- **명세서 ↔ CLAUDE.md 미해결 충돌 — 팀 확정 전 현행 유지**: ① 원화 환산 소스: CLAUDE.md는 업비트 KRW-ETH 고정(의도적 선택·피치 소재), 명세서는 USDT 기준가×환율(단일 API 의존 제거) — 절충안은 업비트 primary + 폴백 소스 설정 ② 캔들 최소 간격: 현행 1m 원본(명세서의 15m은 1m 롤업으로 충족 가능) ③ 가격 갱신 단위: 현행 스왑 이벤트별(Sync), 명세서는 블록 단위(펌핑-덤핑 조작 방어) — P1 인덱서 확장 때 결정, 채택 시 지표 정의 개정

---

## 체인 정보

| 항목 | GIWA Sepolia |
|---|---|
| Chain ID | 91342 |
| RPC | https://sepolia-rpc.giwa.io (rate-limited, 프로덕션 사용 금지) |
| Flashblocks RPC | https://sepolia-rpc-flashblocks.giwa.io |
| 통화 | ETH |
| 익스플로러 | https://sepolia-explorer.giwa.io (Blockscout) |
| 블록타임 | 1초 |

- 퍼블릭 메인넷 **미출시**. 가속 선정팀만 화이트리스트 프라이빗 메인넷 접근
- OP Stack 기반 옵티미스틱 롤업, 단일 시퀀서
- 공개 RPC는 레이트리밋 → 인덱서는 Nodit(람다256) 엔드포인트 사용
- Blockscout API는 토큰 메타/홀더/트랜스퍼만 제공. **가격·거래량·유동성은 직접 계산해야 한다**

---

## 스택

- 모노레포: pnpm workspace
- 인덱서: Ponder (viem 기반, 리오그 처리 내장)
- DB: PostgreSQL. 캔들은 `candles_1m` 원본 → 상위 간격 롤업
- API: Hono 또는 Next.js Route Handler, Redis 캐시
- 프론트: Next.js App Router + TypeScript
- 체인 연동: viem / wagmi (커스텀 체인 정의)
- 테이블: TanStack Table, 차트: lightweight-charts
- 실시간: Nodit Webhook/Stream 또는 SSE. 전체 리페치 금지, 델타만 푸시

---

## 디렉토리

```
apps/
  web/          프론트엔드
  api/          가격 API 서버
packages/
  indexer/      Ponder 인덱서
  contracts/    Foundry 프로젝트
  shared/       타입, 지표 계산 로직 (인덱서/API 공용)
config/
  chains.ts     체인 파라미터 단일 소스
scripts/
  seed/         데모 토큰 발행 + 스왑 봇
```

---

## 지표 정의

여기서 틀리면 전부 틀린다. 새 지표를 추가할 때는 반드시 이 섹션에 정의를 먼저 적는다.

### 가격
- V2: `reserve1 / reserve0`, 양쪽 decimals 보정 필수
- 스왑 이벤트마다 재계산해서 캔들에 반영. 구현: 같은 tx 에서 Sync 가 Swap 보다 먼저 나오므로(V2 `_update` 순서), Sync 핸들러가 준비금·가격을 갱신하고 Swap 핸들러가 그 가격으로 체결·캔들을 기록한다 (`packages/indexer`)
- 캔들 저장: `candles_1m` 원본과 1h/1d 롤업을 스왑 시점에 같은 트랜잭션으로 upsert. 일봉 버킷은 00:00 UTC = 09:00 KST — 업비트 일봉과 같은 경계라 그대로 쓴다
- 캔들 open: 버킷 신규 생성 시 **체결 직전 가격**(`pairs.prevPriceWei` — 직전 Sync 이전 가격)을 쓴다. 체결 후 가격을 쓰면 버킷 첫 체결의 가격 변동이 변동률에서 통째로 빠진다(저활동 페어에서 하루 1체결이면 변동률이 항상 0%가 되는 편향)
- `/stats`: 당일 1d 캔들이 없으면 0.00%·거래 0 같은 확정값 대신 **404(데이터 없음)** — 실제 무거래와 백필 중·인덱서 랙을 구분한다. 웹은 null 로 받아 지표를 감춘다
- **모든 화폐 계산은 bigint.** float 금지. 표시 직전에만 문자열로 변환

### 스왑 견적 / 수수료 (2026-07-27 단일화)
스팟가가 아니라 **실제 체결값**을 다루는 계산. 산식 단일 소스는 `packages/shared/src/amm.ts` — 화면에서 다시 구현하지 않는다.

- 견적: `getAmountOut(amountIn, reserveIn, reserveOut)` — 컨트랙트 `getAmountOut`과 같은 식. 준비금·입력이 0 이하면 0(견적 불가)을 반환하고 판단은 호출부가 한다
- 수수료 절대액은 레그마다 분모가 다르다. 매수는 지불액에서 바로 떼이므로 `feeFromAmountIn`(÷1000), 매도는 **수령 ETH가 이미 차감된 값**이라 `feeFromAmountOut`(÷997)으로 역산한다. 둘을 섞으면 매도 수수료가 과소 표시된다
- 포인트 평가액은 스팟가가 아니라 **전량 매도 시뮬레이션**(`getAmountOut`)이다 — 얕은 풀에서 스팟가를 쓰면 평가액이 부풀려져 등급을 조작할 수 있다 (명세서 §2.5)
- 수수료율을 바꿀 때(메인넷 총 0.8% 전환, P2) **`amm.ts`만 고친다.** 화면마다 흩어져 있으면 한쪽만 고쳐져 거래 견적과 포인트 등급이 서로 다른 수수료로 계산된다 — 조용히 틀리는 종류다
- 이 산식들은 `WeiAmount` 브랜드를 쓰지 않는다. 페어의 어느 쪽이냐에 따라 ETH wei일 수도 토큰 최소단위일 수도 있고 입출력이 전부 같은 단위라, 브랜드가 막아줄 혼동이 없다(브랜드의 일은 wei ↔ 원화 방지 — 절대 규칙 1)

### USD / KRW 환산
- 온체인에 오라클 없음. 기준 풀(WETH-테스트스테이블) 하나를 quote로 고정
- ETH/KRW는 업비트 시세 API(`api.upbit.com/v1/ticker?markets=KRW-ETH`)에서 주입, 캐시 TTL 60초
  - 업비트 시세를 기준으로 쓰는 건 타겟 유저에게 익숙한 숫자를 보여주기 위한 **의도적 선택**이다. 피치 소재이기도 하므로 다른 소스로 바꾸지 않는다
- 환산 실패 시 원화 표시를 감추고 ETH 단위로 폴백. **추정치를 확정값처럼 보여주지 않는다**
- 환산 값은 `DisplayKrw`(0.001원 단위 bigint, `packages/shared`)로 다룬다. 표시 규칙: ≥1,000원 정수+콤마 / 100~1,000원 소수 1자리 / <100원 소수 2자리, **항상 내림**(과대 표시 금지). 축약은 만/억 단위(`formatKrwCompact`)

### 시가총액
`totalSupply × price`. 소각 주소와 락업 물량 제외 여부를 결정하고 그 정책을 코드 주석에 남긴다.

### 유동성
풀이 보유한 quote 토큰 잔고 × 2 (V2 기준)

### 거래 수 / 참여 인원 (2026-07-27 정의 확장)
활동 원장(`activities`) 기준 — **매수·매도·유동성 공급·회수 전부**를 센다. 체결만 세면 유동성 공급자가 지표에서 빠진다.
- 거래 수 = 윈도우 내 활동 건수
- 참여 인원 = 윈도우 내 **distinct `tx.origin`**. 라우터 경유 때문에 `msg.sender`를 쓰면 라우터 주소 하나로 집계된다
- 거래대금은 여기 포함하지 않는다 — 스왑 체결액만 (유동성 공급은 거래가 아니다)

### 보유 분포 (온체인 분석 v0, 2026-07-26)
- 발행자 물량: `balanceOf(issuer) ÷ totalSupply`. issuer = TokenIssued 로 온체인 등록된 주소 (추론이 아니라 등록)
- 상위 10 집중도: 잔고 상위 10개 지갑 합산 ÷ totalSupply — **인프라 주소(유동성 페어) 제외**
- 홀더 수: Blockscout `holders_count`
- 데이터 원천: 홀더 목록·수는 Blockscout API(60초 캐시), 발행자 잔고는 RPC 직접 조회. 실패 시 자리표시 폴백
- 홀더 관계도(버블맵, 2026-07-26 구현): **Transfer 전수 원장 기반** — 인덱서가 발행 토큰 Transfer 를 전수 수집(`holders` 잔고 델타 + `transfer_links` 에지). 에지 = 두 지갑 간 직접 전송(민트 제외), 클러스터 = 연결 성분(union-find) — **전체 전송 참여자 위에서 계산 후 홀더로 투영**(잔고 0 경유 지갑으로 이어진 간접 연결 포함). 인프라 주소(페어·팩토리·라우터)는 성분 계산에서 제외 — 안 거르면 전부 한 덩어리가 된다. 발행자와 같은 성분 = "발행자 연계" 표시
- 인사이더·스나이퍼 비중은 취득 이력 산식(P1) 전제 — 그 전에는 표시하지 않는다(추정 금지)

### 거래량 / 변동률
- 거래량: 윈도우 내 스왑의 quote 토큰 환산 합계
- 변동률: 캔들 open 대비 종가. **요청 시점에 스왑 원장을 스캔하지 않는다**

### 체인 활동 (분석 탭, 2026-07-29 정의)
나루 인덱서가 아니라 **기와체인 전체**를 말하는 지표들. 원천은 Blockscout 공개 기록이고, 나루 집계와 별개라는 것을 화면에 명시한다. 리더는 `apps/web/lib/chain.ts`.

- **하루 거래** = 최근 **완결된** 7일의 일별 거래 수 평균. 진행 중인 오늘(UTC)은 `getDaily`가 잘라내므로 평균과 차트가 같은 모집단을 본다 — 반나절치를 하루로 세면 흐름이 실제보다 꺾여 보인다
- **일별 거래 수** = 최근 30일, 오래된 → 최신 순. 위와 같은 이유로 오늘은 빠진다
- **거래 수수료** = 최근 검증된 트랜잭션 **한 페이지**(Blockscout 기본 크기)의 `fee.value` **중앙값**
  - 평균이 아니라 중앙값인 이유: 컨트랙트 배포 한 건이 평균을 수십 배로 끌어올려 "이 체인 비싸네"로 읽히게 만든다. 중앙값이 흔한 거래 한 건의 체감에 가깝다
  - `fee.value`는 **L2 실행분 + L1 데이터 수수료 합산**이다(2026-07-29 산술 확인: `gas_used × gas_price + l1_fee`와 일치). OP Stack에서 L2분만 쓰면 과소 표시된다
  - 표본이 한 페이지라 모집단이 Blockscout 페이지 크기에 좌우된다 — 정확한 중앙값이 아니라 **최근 체감치**로 다룬다
- **블록 간격** = `average_block_time ÷ 1000` (Blockscout은 밀리초로 준다)
- 원천이 죽으면 해당 지표만 null로 비우고, 전부 null이면 보드째 감춘다. 0이나 "-"로 채우지 않는다

### 손익 / 수익률 (내 자산 PnL, 2026-07-29 정의)
기획서 §4.5("보유·거래 내역·손익 — 온보딩 마지막 단계의 안심 장치") 구현. 산식 단일 소스는 `packages/shared/src/pnl.ts`.

- **총손익 = 평가금액 − 순투입**, 순투입 = 총매수 WETH − 총매도 WETH
- **실현·미실현을 나누지 않는다.** 나누려면 "판 수량이 짊어진 원가"를 정해야 하고 그건 이동평균이냐 FIFO냐를 강요하는데, 합쳐 보면 그 선택 자체가 사라진다 — 넣은 돈·뺀 돈·남은 값어치만으로 답이 나온다. 부분 매도, 전량 매도 후 재매수도 분기 없이 맞는다
- **기준 통화는 ETH(wei)다.** 원화는 표시 직전에 현재 환율로 환산한다. 온체인에 과거 환율 이력이 없어 "매수 시점 원화 원가"는 만들 수 없다 — 만들면 추정이 된다(절대 규칙 1)
  - 그래서 화면에 **"ETH 기준"을 반드시 명시한다.** ETH가 오르면 토큰을 안 팔았어도 원화 손익이 움직이는데, 라벨이 없으면 "원화로 산 것 대비 손익"으로 읽힌다
- **평가금액**은 스팟가가 아니라 `getAmountOut` 전량 매도 시뮬레이션 — 내 자산·포인트가 같은 기준을 쓴다
- **수익률(ROI) = 총손익 ÷ 총매수.** 분모가 순투입이 아닌 이유: 원금 이상 회수한 지갑은 순투입이 0이나 음수가 되어 수익률이 폭발하거나 부호가 뒤집힌다. 총매수를 쓰면 "넣은 돈 대비 얼마"라는 뜻이 끝까지 유지된다. 매수 이력이 없으면 표시하지 않는다
- **원장은 `trades.origin`(tx.origin) 기준.** 라우터 경유라 `msg.sender`를 쓰면 안 된다(§거래 수 / 참여 인원과 같은 이유)
- **원장 수량과 잔고가 정확히 같을 때만 손익을 낸다.** 어긋나면 양방향 모두 틀린다 — 잔고가 더 많으면 전송으로 받은 물량이라 투입 없이 평가액만 늘어 과대 계상되고, 원장이 더 많으면 전송으로 내보낸 물량이라 남은 수량에 전액 투입이 얹혀 있지도 않은 손실이 찍힌다. 어느 쪽이든 해당 자산은 손익을 **표시하지 않는다**(추정 금지). 인덱서 시작 블록 이전 거래도 같은 취급
- **합계는 표시용 행이 아니라 보유 목록 전체에서 낸다.** 전량 매도해 잔고가 0인 자산도 실현 손익을 갖는데(평가 0 − 음수 순투입), 표에서 걸러낸 행으로 합계를 내면 그 이익이 조용히 사라진다
- 손익은 **이미 일어난 사실**이라 기획서 §8-1의 "기대수익 표현 금지"에 걸리지 않는다. 반대로 미래 배분의 예상 금액은 금지 대상이다 — 이 선을 넘지 않는다

---

## 컨트랙트

- Foundry 사용
- Uniswap V2 포크(Factory / Pair / Router) + 토큰 발행 게이트
- **런치패드는 제품이다 (2026-07-21 팀 결정).** 기와 생태계에 아직 런치패드가 없다 — 선점한다. 단, 퍼미션리스가 아니라 **신원 검증 발행**이 정체성이다
  - 검증된 신원(도장 어테스테이션 / GIWA ID)만 발행 게이트를 통과한다. 검증 없는 즉시 상장 UI는 만들지 않는다
  - 발행 동선은 신청 → 검증 → 상장 흐름이다. "즉시 풀 생성" 같은 카피는 쓰지 않는다
  - 데모 단계의 프론트 발행은 시뮬레이션이며 화면에 명시한다. 온체인 발행은 컨트랙트 배포 후 연결한다
  - 발행 게이트 컨트랙트(TokenFactory)는 데모 시드 생성과 런치패드 제품이 같은 경로를 쓴다
- 배포 후 **Blockscout verify까지 완료해야 제출물로서 의미가 있다**
- 배포 주소는 `config/chains.ts`에 기록, 코드에 흩뿌리지 않는다
- 심사 제출용이므로 컨트랙트에 한글 주석으로 의도를 남겨둔다

---

## 코딩 컨벤션

- TypeScript strict. `any` 금지
- 금액은 bigint, 화면 표시 직전에만 포맷팅
- 인덱서는 멱등하게. 같은 블록을 두 번 처리해도 결과가 같아야 한다
- `eth_getLogs`는 블록 범위를 청크로 나눠 호출한다. 전체 범위 단발 호출은 거부당한다
- 커밋 메시지는 한글 가능, 코드 식별자는 영문
- 프론트 상태는 React state로. 브라우저 스토리지는 **지갑 세션 표식 하나만** 예외다(2026-07-29)
  - 예외를 둔 이유: 사용자가 "연결 해제"를 눌렀다는 사실은 우리만 알 수 있다. 지갑이 쥔 승인 상태만 보고 복구하면, `wallet_revokePermissions` 미지원 지갑에서 해제를 눌러도 새로고침에 세션이 되살아나 공용 PC 에서 앞사람 지갑이 다음 방문자에게 노출된다
  - 저장 범위는 **고른 지갑의 rdns 하나**다. 주소·잔고·서명 등 식별 가능한 값은 넣지 않는다(기획서 §8-6 개인정보 비보유). 주소는 매번 `eth_accounts` 로 새로 받는다
  - 표식과 지갑 승인이 **둘 다** 있어야 세션이 산다. 한쪽만으로 복구하지 않는다
  - 이 예외를 다른 상태로 넓히지 않는다. 화면 상태·필터·정렬 같은 것은 여전히 React state 로만 둔다

---

## 프론트 구조 규칙 (apps/web, 2026-07-27 확정)

SOLID 리팩토링 때 정한 규칙. 새 화면·컴포넌트를 만들 때 그대로 따른다.

### 레이어와 위치

```
app/          라우트 — page.tsx 는 조립 전용
components/   시각 컴포넌트 — 도메인 폴더로 구분
  ui/         2개 이상 도메인이 쓰는 공용 (copy-address, asset-avatar, verified-badge …)
  layout/     사이트 크롬 (site-header, site-footer, hero …)
  asset/ launch/ portfolio/ points/ feed/ guide/ docs/ wallet/
hooks/        재사용 클라이언트 로직 (use-escape-key, use-async-effect, use-poll …)
contexts/     React 컨텍스트 (wallet-context, search-context)
lib/          서버·데이터 로직 + api-types.ts
```

### 규칙

- **page.tsx 는 조립 전용** — 리터럴 카피(제목·문단 원문)를 넣지 않는다. 카피는 도메인 컴포넌트로 뺀다. `metadata` 의 문자열은 Next.js 가 라우트 파일에 강제하므로 예외
- **새 컴포넌트는 자기 도메인 폴더에.** 두 도메인 이상에서 쓰게 되면 그때 `ui/` 로 승격한다 — 처음부터 ui 에 넣지 않는다
- **승격 시점 = 두 번째 도메인이 필요로 하는 바로 그 변경에서.** 도메인 간 임포트가 금지라 다른 도메인 컴포넌트를 쓰는 유일한 합법 경로가 승격이다: 같은 커밋에서 `ui/` 로 이동 + 기존 임포트 수정까지 끝낸다. "임시로 크로스 임포트, 나중에 이동"은 금지 — 임시가 영구가 된다. 단, 옮기기 전에 한 번 자문: 두 번째 도메인이 정말 같은 컴포넌트가 필요한가, 아니면 비슷해 보이는 자기 도메인 물건이 필요한가 (도메인 색이 짙은 컴포넌트를 억지로 공용화하면 props 분기만 늘어난다)
- **도메인 → 도메인 직접 임포트 금지.** 조합은 page 레벨에서 한다. 허용된 유일한 예외: feed → guide (도크가 가이드 오버레이를 호스팅, 의도된 설계)
- **배럴(index.ts) 금지** — 경로를 직접 임포트한다 (순환 임포트 방지)
- **내부 API 응답 타입은 `lib/api-types.ts` 가 단일 소스** — 라우트 핸들러는 응답 객체에 이 타입을 명시하고, 화면은 같은 타입을 임포트한다. 컴포넌트 파일에 와이어 타입을 선언하지 않는다 (PortfolioResponse 2중 정의 드리프트 재발 방지)
- **컴포넌트·함수는 책임 하나씩.** 상태·로직은 도메인의 메인 컴포넌트가 쥐고, 분리한 서브컴포넌트는 props 로만 받는다. 단, props 가 8개를 넘어가면 분리가 결합을 늘리는 신호 — 같은 파일 서브컴포넌트로 남긴다
- **훅 추출은 중복 2곳 이상일 때만.** 1회성 로직을 미리 훅으로 만들지 않는다
- 한 도메인 폴더가 10개를 넘으면 화면 단위 하위 분리를 검토한다
- **반응형은 반응형 클래스(sm:/md:) 한 트리가 기본** (2026-07-31 확정). 뷰포트별 컴포넌트 포크(`*-table` ↔ `*-cards`)는 UX 구조 자체가 갈라질 때만 — 포크하면 두 벌을 기능 동기화해야 한다. 포크 시 전환은 자식이 숨김 클래스를 품지 말고 부모가 `ui/responsive.tsx`의 `<Responsive desktop mobile>`로 조립한다 (SSR 첫 페인트 보장 — 훅 분기 금지, 이유·주의는 responsive.tsx 주석). 뷰포트별 "동작" 분기만 `hooks/use-media-query.ts`. 페이지 좌우 여백은 `px-page` 유틸리티 — `px-8` 하드코딩 금지

---

## 명령어

```bash
pnpm dev            # 전체 개발 서버
pnpm indexer:dev    # 인덱서만
pnpm seed           # 데모 토큰 발행 + 스왑 봇 기동
pnpm typecheck
forge test          # packages/contracts 내부
```

---

## 현재 단계

가속 2차 제출 준비. 우선순위 순서:

1. 컨트랙트 배포 + verify
2. 데모 토큰 발행 + 스왑 봇 상시 가동 ← **빈 테이블로 제출하면 끝이다**
3. 인덱서 + 가격 API
4. 터미널 첫 화면
5. 데모 영상, 피치덱, 기술 문서

완성도보다 **돌아가는 뼈대 + 선명한 방향**이 먼저다.

---

## 보안

시드 봇 때문에 키를 여러 개 굴리게 된다. 사고 나기 쉬운 지점이다.

- 개인키 / 니모닉 / API 키를 커밋하지 않는다. 저장소에는 `.env.example`만 둔다
- **배포 지갑과 봇 지갑을 분리한다.** 봇 키는 테스트넷 전용이고 어떤 경우에도 메인넷 자산을 쥐지 않는다
- 봇 키를 프론트 번들이나 클라이언트 사이드 코드에 절대 넣지 않는다
- 심사 제출용 저장소를 공개할 때 커밋 히스토리에 키가 남아 있는지 먼저 확인한다

---

## 인덱싱 참고

주요 이벤트 topic0 (keccak256 계산값):

```
PairCreated(address,address,address,uint256)
  0x0d3648bd0f6ba80134a33ba9275ac585d9d315f0ad8355cddefde31afa28d0e9

Swap(address,uint256,uint256,uint256,uint256,address)   # V2
  0xd78ad95fa46c994b6551d0da85fc275fe613ce37657fb8d5e3d130840159d822

Sync(uint112,uint112)                                   # V2 준비금 갱신 — 가격 재계산 트리거
  0x1c411e9a96e071241c2f21f7726b17ae89e3cab4c78be50e062b03a9fffbbad1

Transfer(address,address,uint256)
  0xddf252ad1be2c89b69c2b068fc378daa952ba7f163c4a11628f55a4df523b3ef
```

새 이벤트를 추가할 때는 시그니처를 추측하지 말고 직접 keccak256을 계산해 확인한다.

---

## 용어

이 프로젝트 고유 용어라 문맥 없이는 알 수 없다.

| 용어 | 설명 |
|---|---|
| 업저씨 | 업비트에서 거래대금이 큰 현물 위주 홀더. 온체인 경험이 거의 없다. **이 프로젝트의 타겟** |
| 기와(GIWA) | 두나무의 웹3 인프라 브랜드. 기와체인 + 기와월렛으로 구성 |
| 가속(GASOK) | 두나무 개발자 지원 프로그램. 이 프로젝트의 제출처 |
| 도장(Dojang) | 오프체인 데이터를 온체인에서 검증하는 기와 어테스테이션 레이어 |
| GIWA ID | ENS 기반 네임. 양도 불가 SBT이며 KYC 검증 주소에 종속된다 |
| 보자기(Bojagi) | 기와의 영지식증명 프라이버시 프로토콜 |
| 두나무 | 업비트 운영사. 기와의 개발·운영 주체이자 심사 주체 |

---

## Claude에게

- 새 기능을 제안할 때 위 절대 규칙 6개와 충돌하는지 먼저 확인한다
- 타겟 유저가 업저씨라는 걸 잊지 않는다. 크립토 네이티브용 편의 기능은 대부분 불필요하다
- 지표 계산 코드를 수정할 때는 "지표 정의" 섹션도 같이 업데이트한다
- 결정을 임의로 바꾸지 말고, 바꿔야 한다고 판단되면 이유와 함께 먼저 묻는다
- 마감이 촉박하다. 완벽한 추상화보다 동작하는 구현을 우선한다
