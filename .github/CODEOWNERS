# 코드 소유자 - PR 이 열리면 여기 적힌 사람에게 리뷰가 자동 요청된다.
#
# 문법 주의: .gitignore 와 비슷해 보이지만 **마지막으로 매칭된 규칙 하나만** 적용된다
# (먼저 쓴 규칙이 아니라 나중 규칙이 이긴다). 그래서 좁은 경로일수록 아래에 적는다.
#
# 지금은 전 경로를 한 사람이 받는다. 경로별로 나누고 싶어지면 이 줄 아래에 추가한다.
*   @0xSHKWON

# 이 파일은 main 브랜치 보호와 묶여 **실제로 머지를 막는다** (2026-08-26 활성화).
# 저장소가 퍼블릭으로 전환되면서 Free 플랜 제약이 풀렸고, main 에 다음이 걸려 있다:
#   PR 필수 + 코드오너 승인 1개 + CI 필수 체크(typecheck·contracts) + 직접 push 금지.
# 즉 여기 적힌 사람의 approve 와 CI 초록불 없이는 main 에 들어갈 수 없다.
# (관리자는 우회 가능 - 코드오너 본인 PR 은 self-approve 가 안 되기 때문에 열어둔 것)
