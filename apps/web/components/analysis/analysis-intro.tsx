/**
 * 분석 라우트 인트로 - 화면 제목·리드를 모두 안 그린다(2026-09-01 팀 결정).
 * 이미 "분석" 탭을 눌러 들어온 화면이라 같은 말을 반복할 이유가 없고, 제목
 * 없이 리드만 남기면 붙을 곳 없는 고아 문장이 된다 - 섹션(기와체인 활동·나루
 * 거래 흐름)이 각자 제목·리드를 쥔다. h1 은 스크린리더·SEO 용으로만 남긴다
 * (라우트당 h1 하나 유지 - page 는 조립 전용이라 리터럴 카피는 여기 둔다).
 */
export function AnalysisIntro() {
  return <h1 className="sr-only">분석</h1>;
}
